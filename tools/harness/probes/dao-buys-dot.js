(()=>{const c=document.querySelector('canvas[aria-label*="DAO buys"]');if(!c||!window.Chart)return null;const ch=window.Chart.getChart(c);if(!ch)return null;const meta=ch.getDatasetMeta(2);const els=meta.data;if(!els.length)return null;let best=els[0];for(const e of els)if(e.options.radius>best.options.radius)best=e;const r=c.getBoundingClientRect();return{x:r.left+best.x,y:r.top+best.y,n:els.length,r:best.options.radius};})()
