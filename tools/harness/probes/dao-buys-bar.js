(()=>{const c=[...document.querySelectorAll('canvas')].find(x=>(x.getAttribute('aria-label')||'').startsWith('PTGC bought per'));if(!c)return null;const ch=window.Chart.getChart(c);const m=ch.getDatasetMeta(0);let best=null;m.data.forEach((e,i)=>{const v=ch.data.datasets[0].data[i].y;if(!best||v>best.v)best={e,v};});const r=c.getBoundingClientRect();return{x:r.left+best.e.x,y:r.top+best.e.y+4};})()
