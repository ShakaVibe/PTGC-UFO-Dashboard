# 2026-09-26 — Holders cards redone (Socials + the ⓘ 📷)

Shaka: "another graphic update — the HOLDERS. Little i in the holders box on both dashboards and in
the socials. Make these look a lot better like the other graphics lately. Mock-ups first."

## Mock-ups (six rounds, PNGs rendered in headless Chromium from a standalone HTML, real numbers from
`data/holder-history.json` + `data/token-allocation.json` as of 2026-09-25)

1. Three directions: A combined card (cyan), B one-token card (cyan), C = A in gold/green. Wide + Tall.
2. Shaka: no accent bars on the tiles ("keep it clean"); the ⓘ modal's "Today vs Daily Growth Rate"
   (per-day average + today-vs-average %) was missing — folded into each period tile; bigger logos.
3. Title icon: the two blue 👥 heads → two Grays heads (first a drawn SVG, then Shaka's own
   `logos/combined/14_Two_Color__Alien_2_Transparent.png`, doubled up).
4. Cyan → silver. 5. Silver → **gold→green on the combined card; the one-token card in that token's
   colour (PTGC all gold, UFO all green)**. "Make it live."

## Built (index.html)

- Module-scope block `HOLDERS — SOCIALS CARD + ⓘ CARD`, just above `DashboardSkeleton` (gotcha 31):
  `HOLD_SHAPES` / `HOLD1_SHAPES` / `HOLD_THEME` / `HOLD_COMBO`, `holdersRate` (= the ⓘ modal's
  calcRatePct), `holdersTierCounts` (an all-zero tier object = unknown → "—"), `holdersSeries30`
  (snapshotsFor, epoch-filtered), `HoldersSpark` (inline SVG trend, `React.useId` for the gradient ids),
  `HoldersAliens` (two copies of the cropped head), `HoldersLogo`, `HoldersTodayChip`,
  `HoldersPeriodTiles` (7D/30D/90D: "then", net, per-day avg, today-vs-avg %; "Gathering… / not enough
  history yet" when a period has no snapshot), `HoldersTierRow`, `HoldersDualBackdrop` (gold top-left /
  green bottom-right, the combined logo watermark, gold→green frame), `HoldersTokenPanel`,
  `HoldersSocialCard` + `HoldersSocialModal` (combined; `CardLoadState` overlay for loading / error +
  Retry), `HoldersSingleCard` + `HoldersSingleModal` (one token; `zIndex={60}` so it stacks on the ⓘ
  modal and Escape closes only the card — checked).
- CSS: `.metallic-green`, `.gold-green-text` (next to `.metallic-gold`).
- New image `logos/combined/14_Two_Color__Alien_2_head.png` (488×561, the alien cropped to its alpha
  bbox — the 2000² original is ¾ transparent margin, useless as an inline icon).
- Socials → Holders: the old 1200×675 html2canvas card (two Chart.js canvases drawn by a
  requestAnimationFrame retry loop) is gone with its four refs and its effect; the same loader feeds
  `HoldersSocialModal`. Loader change: a count or tier set neither the files nor the live fallback
  could supply is now **null** (was `||0` / `{0,0,0,0,0}` — the new card would have printed five
  zeros and "0 holders"; the old one didn't show tiers at all, which is why it never bit).
- ⓘ Holder Analytics modal: new `onShare` prop → a 📷 beside the ×; Dashboard state
  `showHoldersSingleModal`; the card reads `holders` / `holdersByPeriod` / `holderTiers` — the exact
  numbers the modal prints, nothing fetched.

## Harness (cloud workspace, Chromium at /opt/pw-browsers)

`npm run compile` ok; `npm run css` (no new classes). Runs, all clean:
- `node run.js "#/ptgc" 1440 1000 out/hold "wait=5000;click=button:has-text('Socials'):visible;wait=1200;click=button:has-text('Holders'):visible;wait=3000;shot=wide;click=button[aria-label='Tall card, 4:5 for phones'];wait=1500;shot=tall"`
  — 23,407 · +28 7D / +126 30D; PTGC 18,101 (+4 today; 7D +23 / 30D +100 / 90D +291; 3.3/day ▲21.7% …);
  UFO 5,306 (7D +5 / 30D +26 / 90D "Gathering…"); tiers 1/14/101/710/17,266 and 0/19/103/517/4,659.
- ⓘ → 📷 on PTGC and UFO: `click=button[aria-label='Holders by period']:visible;…;click=button[aria-label='Share holder analytics as an image']` — card numbers = modal numbers; Escape leaves the modal open.
- `SLOW_HISTORY=8000` → "Loading data…" overlay over a dashed card; `probes/fetch-fail-all.js` after load
  → the cached history/allocation still answer (error branch is `CardLoadState`, same as the a18 cards);
  375×812 phone, both shapes, scale like the other cards.
- **Harness fix (`tools/harness/run.js`):** the DexScreener stub gave every pair
  `pairCreatedAt = now − 1062 d`, PTGC's age — and `index.html` learns `ufoLaunchMs` from the youngest
  UFO pair, so in the harness UFO's history epoch reached back into the OLD contract and the UFO ⓘ modal
  / card read 90D "6,597 then ▼ −1,291". UFO pairs are now born 2026-07-08 in the stub. Live is
  unaffected (DexScreener's real createdAt is July 2026) — a harness lie, not a site bug.

## Owed

The live look on a phone: Socials → Holders (Wide + Tall), then PTGC ⓘ → 📷 and UFO ⓘ → 📷. The
Socials list still says "👥 Holders" — Shaka didn't ask for the button; say if the alien pair should go
there too.

## Round 2 (afternoon) — Shaka's live look

1. "Clicked the ⓘ and it's still the old graphic." The ⓘ opened the 2025 modal with the new card one tap
   further behind its 📷. Now the ⓘ opens `HoldersSingleModal` directly; the old modal's 7D / 30D / 90D trend
   toggle moved into the card's toolbar (`days` state → `holdersSeries(tkn,cur,days)`, the pill and the
   "+n in N days" line follow it; UFO 90D reads "76D AGO" on the axis and "— in 90 days" until Oct 6).
   `HolderAnalyticsModal` + `PctBadge` deleted (≈250 lines) — the card prints every number they did.
2. "The boxes at the bottom overlap the border" — Wide and Tall, on his Mac. Apple Color Emoji and the real
   font stack run taller than the harness's Noto/Linux metrics, and the cards had 0–10 px to spare. Fixed
   with real slack rather than a metric guess: smaller trend boxes (single 132 / 140, combined 148 / 96),
   tighter tier tiles (`tpad`), emoji glyphs in fixed-height boxes, every spacer `minHeight:0`, and
   **`probes/card-slack.js`** — reports the px left under the lowest content element of each card on screen
   (negative = overflow). After: single Wide 34, single Tall 44, combined Wide 30, combined Tall 44.
   Run it after any change to a card's sizes; want ≥ 25 px, because a Mac eats ~10.
3. Socials hub: a **Holders** button in the PTGC and UFO columns too, on row 7 so all three line up with
   Combined → Holders (PTGC: after DAO Buys; UFO: after KPI Report). Same cyan look as the combined one; each
   opens `HoldersSingleModal` for its token via the usual `setSocialReturn` + `onSwitch` pattern, and closing
   goes back to Socials (`closeModalAndReturn`; from the ⓘ it just closes). Harness: opened PTGC Holders from
   the UFO dashboard → switched, gold card; Escape → Socials.

## Afternoon, part two — Live Feed redesign (the "Abduction Deck")

Shaka: "make it look a lot better", a new painted background (the ship over a golden city, beam down the
centre), a rough mock with five framed boxes around the logo, green dollar figures in the ledger, the LP
boxes carrying the partner token's logo instead of a droplet, and "make sure there is still motion".
Mock-ups first (desktop PTGC + UFO, then iPhone, then the LP logos), then built.

**A skin, not a rebuild.** `AbductionDeckModal`, `DeckPods`, `DeckRow`, `DeckScanline`, the dispatch
animation (`.deck-a.on`, chips, sparks, ping, scan ring, hover, dashed links, sweep, motes) are untouched;
only what they draw on changed:
- `logos/livefeed/deck-bg.jpg` (1600×698, q86, 300 KB — the source cropped so the beam is exactly centred)
  is the sky (`.deck-art`, 430 px tall, was a 380 px starfield). UFO = the SAME file with
  `hue-rotate(52deg)`; no second image. The old ground grid is gone; the drawn beam wedge is faint (the art
  has the real beam) but still exists because the sweep and the motes clip to it.
- Pods: glass boxes (`min-width` 190 / 162 px, rounded, token-coloured border + glow), an icon before the
  name — the bucket's emoji, Stakers = 🪙, and for an LP bucket the partner logo via `bucketLogoSrc`
  (`DeckPodLogo`, falls back to 💧 if the CDN image fails), metallic totals. `DECK_POD_POS` 5/6 rewritten
  for the taller sky (boxes around the ship, clear of the beam); 4/3/2/1 layouts unchanged.
- Ship at `top:150` with a halo + ring; the Generated readout on a glass pill (`.deck-readout`).
- Ledger: header "TIME ↓ / TYPE / CONTACT / TX / AMOUNT lifted @ 5%", amounts green `#4ade80` (Shaka),
  split cells in the token colour, rounded type chips.
- Phone (≤640): boxes in a strip over the art (five across / three-by-two), ship + readout under them, and
  the tape is TWO LINES per contact (trade + green amount, then the split labelled by bucket name) instead
  of the 1040 px sideways scroll — `.deck-row` becomes flex-wrap there, `.deck-tape{min-width:0}`.
- Header title metallic in the token colour; deck frame border/glow stronger.

Harness: `DECK_LOGS=8` PTGC 1440, `DECK_LOGS=6` UFO 1440, PTGC + UFO at 375×812 (top + tape), `REDUCED=1`.
Lift chips, ping and count-ups visible mid-flight in the shots. Console clean. **Owed: the live look on the
Mac and the phone — PTGC and UFO, watch a contact land.**
