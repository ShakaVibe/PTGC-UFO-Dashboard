# 2026-09-18 — pipeline cadence measured (throttled), stale thresholds; Audit II calculators a5–a9

## Morning — the Sep 18 cadence check (a29 follow-up)

GitHub is throttling even ONE hourly workflow. Actions API for `data-pipeline.yml` (read from the
local VM; the cloud proxy blocks api.github.com): 6 runs total — the by-hand run 14:29 UTC on
the 17th, then scheduled runs created at 18:55, 22:03, 00:04, 04:32, 09:34 UTC. Gaps 2 h – 5 h 02 m,
about five runs a day, every one green, each ~12 min. The cron asks for :07; the events are
simply delivered when GitHub gets to them. So, as the 17th's note said: don't fight it, move the
site's stale thresholds to match.

- `index.html` `VALUE_GEN_STALE_MS` 3 h → **6 h** (one missed run, then the live-scan fallback).
- `index.html` `BURN_HISTORY_STALE_MS` 6 h → **8 h** (amber "as of" only past a missed run;
  `BURN_HISTORY_MAX_AGE_MS` stays 7 days).
- Left alone, on purpose: the DAO Buys 6 h "stale" pill (the modal re-scans the chain on open
  anyway) and `charts.html` `STATUS_STALE_MS` = 2 h — the charts step is gated at 2 h of file age,
  so with 3–5 h between runs the Charts page will read amber "as of …" for most of the day. It is
  an honest label; Shaka chose not to relax it today. If it grates, raise it to 6 h.
- The a30 / a32 migrations landed exactly as planned in the first scheduled run (`288b02330`):
  `ptgc-burns-2026.json` deleted, `2026-h1` + `2026-h2` present, `ufo-ptgc-burns-cache.json`
  (2.4 MB) next to a 302 KB `ufo-ptgc-burns.json` with `rows` = 2,389 and no legacy `_…Cache`
  keys. `dao-buys.json` `generatedAt` keeps moving (09:46 UTC), 318 buys across both wallets.
- Options if ~5 runs/day is not enough later: an outside cron (cron-job.org or similar) calling
  the `workflow_dispatch` API hourly with a fine-grained token (`actions: write`) — dispatch runs
  start at once (the 17th's manual run did). Needs a token Shaka creates; not done.

Housekeeping: `git fetch` from the bridge left `.git/objects/pack/pack-d8df30…keep` it could not
unlink ("Operation not permitted") — harmless (it just pins that pack), `rm` it when convenient.
The local checkout was 5 data commits behind origin; the usual `git pull --rebase` picks them up.

## Afternoon — Audit II a5–a9, `calculators.html` (+262/−94 lines)

Evidence: `2026-09-16-audit.md` section G. Method: exact-string edits applied in the cloud
workspace (the harness runs there — no Chromium on the local VM), verified, then written back to
the linked folder; md5s checked both ways.

- **a6 — RPC pool + null burn.** `RPC_ENDPOINTS` / `_rpcOnce` / `rpcFetch` ported from
  index.html (three endpoints, transport-only failover, 2-min sticky). New `rpcWord(r)` = the hex
  word of an `eth_call` or `null` (`{result:null}`, a JSON-RPC error, `'0x'`). `fetchBurn` returns
  `null` when either read is null (was `{total:0}` — and `BigInt(bRes.result||'0x0')` never even
  reached the catch). `burn` / `freshOtherBurn` / `moonOtherBurn` are seeded and reset with
  `null`. `CalculatorsTab`: `circulatingSupply = burn ? total − burn.total : null`,
  `supplyUnknown`; X Multiplier `baseMcap`/`finalMcap`, Moon Math `mCirc` / `newMcap` /
  `moonCombinedMcap` / `mMcap` / `calcMoonImpact` / `calcMoonDirectImpact` all `null` → "—"
  (`fmtMcap`/`fmtAbbr` already print null as a dash). Rewards: `circSupply` was `…:1` — an unread
  burn made the user's share `bag / 1`; now `null` → `eligibleSupply` null → share 0 and
  `hasResults` false, with an amber note under "Circ. Supply: —" ("burn could not be read from the
  chain; rewards need it. Reload to retry."). Fresh Capital: `ptgcCirc` / `ufoCirc` /
  `ufoEligibleSupply` null → `calcRewardsSplit` guards `eligSup>0` (no Infinity), `getCreatures`
  already returns `[]` for a null circ.
- **a5 — PLS price.** `fetchPLS` → `dsOnePair(await dsJson(WPLS/DAI pair))?.priceUsd` or `null`;
  `pls` seeded `null`; a failed refetch keeps the last good value (`if(p!=null)setPls(p)`). New
  `dsJson(url)` (null on non-2xx / non-JSON / network, one retry on 429/5xx) and `dsOnePair`
  (both the `{pair}` and `{pairs:[…]}` shapes — the `/pairs/` endpoint answers with `pairs[]`
  now, which is why the old `.pair?.priceUsd` read fell to the $0.00005 constant so often).
  `fetchDex`'s main-pair call and the Rewards ecosystem-box PTGC price use them too — the
  main-pair call can no longer take the whole `fetchDex` down. Header PLS ratio, the X
  Multiplier WPLS row and Moon Math's WPLS core all already guard `pls>0` → "—" / no row.
  Header 24 h change prints "—" without data (was "↑ 0.00%").
- **a8 — Rewards inputs.** `rwdTokenAmount` / `rwdStakedAmount` declared ABOVE their save
  effects (the deps array was `[undefined, token]` — never re-ran on typing; at mount the effect
  wrote '' over the stored bag). Second half, all four save effects: deps are the VALUE only. With
  `[value, token]` every effect re-ran on the token change itself, in the same commit as the load
  effect (which had already moved `inputsTokenRef`), so the old token's bag was written under the
  new token's key whenever the new token had nothing saved — the X Multiplier pair leaked the same
  way. The load effect now resets a field the new token never saved to '' (per-token bags, as the
  comment always promised) instead of leaving the other token's value in it.
- **a9 — keep the calculators mounted across a switch.** `loadedOnce` state; the full-screen
  "Loading Calculators…" gate is `loading && !loadedOnce` (first load only). During a switch the
  page renders with `data` / `burn` null, an amber `role=status` banner under the header
  ("Loading UFO data… your inputs are kept.") and the four "Current Market Prices" pills are a
  module-scope `LiveBadge` (Loading / Unavailable / Live; `loading` is a new `CalculatorsTab`
  prop). Typed X-factor, target, Moon Math amounts, Growth scenario survive; per-token bags reload.
- **a7 — token-switch race.** `let cancelled=false` in the token effect, checked after every
  await and before every setter (data/burn/pls, staking, UFO share rate + pairs, volume), cleanup
  `()=>{cancelled=true}`. Note: in the OLD page the race was mostly masked because the full-screen
  gate unmounted the Switch button on the first tap — a9 makes a second tap possible, so a7 is
  what makes a9 safe. `load()` also has a `.catch` now (loading false, gate lifted) instead of
  hanging on "Loading Calculators…" if `fetchCoingeckoData` ever throws.

**Harness** (`tools/harness`): `DS_UFO_PRICE=n` (UFO calls get their own price and base symbol)
and `SLOW_UFO=ms` (only UFO DexScreener calls wait) in `run.js`; probes `calc-header.js` (header
token/price, X-factor / holding inputs, banner, badges, `grays_calc_*` keys), `calc-type.js`
(React-visible typing via the native value setter; `window.__typeSel` / `__typeVal`),
`calc-sanity.js` (any NaN / Infinity / undefined on the page + the "MCap:" strings). README has the
three commands. Results: race run with the cancel flag stubbed out → header PTGC with UFO's price;
with it → PTGC's price. Holding typed → saved on the keystroke → survives `reload=` → Switch → UFO
box empty, PTGC bag still in localStorage → 777 typed on UFO → Switch back → PTGC shows
1,500,000,000, UFO key = 777. X-factor 7 survives a switch; banner + "Loading" pill mid-switch. All
five tabs walked in normal / `RPC_DOWN=1` / `DS_DOWN=1`: no NaN / Infinity / undefined, no console
errors beyond the expected 503s; X Multiplier "MCap: $3.17M / $6.33M" normally, "MCap: — / —" with
RPC down; Rewards "Circ. Supply: —" + note with RPC down; header "— | — | PLS RATIO —" with
DexScreener down. Tailwind for the harness run was built with `--content ../../index.html,../../calculators.html`
(the config only scans index.html; the live page uses the play CDN so nothing to ship).

Not done: `portfolio.html` still has the single endpoint + `.pair` reads (a12 / a26 / a27 are the
portfolio items). The LV tab (not live) keeps `burn?.total||0` at line ~1398.

## Evening — a36: the Ledger reads one slim file

`ledger.html` pulled ~3.7 MB (four treasury files, three years of rows with full calldata and
internal transactions) to show a 7-month picker. Now:

- **`scripts/fetch-treasury-transactions.js`** — `buildRecent()` / `writeRecent()` write
  `data/treasury-recent.json` after the four full files: both wallets, whole months back to the first
  of the month 7 months ago (`RECENT_MONTHS = 8`, so one spare month beyond the picker), only the
  fields ledger.html reads (`RECENT_TX_FIELDS` / `RECENT_TRANSFER_FIELDS` — `input` stays FULL because
  the multicall output-token scan walks all of it; `internal_transactions`, `gas`, block numbers and the
  spam/verified flags are dropped), newest first, with `schema`, `since` / `sinceIso`, `counts`.
  Compact JSON: **859 KB** (541 txns + 594 transfers) vs 3.7 MB. `--recent-only` rebuilds it from the
  files on disk with no network (harness + after a schema change). Add a field to the lists the moment
  ledger.html starts reading it.
- **`ledger.html`** — `loadFromFiles` fetches `treasury-recent.json` first and uses it only when
  `schema === 1` and `since` ≤ the first day of the oldest month the picker offers; otherwise
  (404, old shape, short window) it logs why and runs the old four-file path, then the API. Footer
  time = the slim file's `lastUpdated`.
- **`data-pipeline.yml`** — `data/treasury-recent.json` in the commit list (the file is protected from
  the bridge's file writer — edited in place with a shell python one-liner; remember that for next time).
- **Harness** — `probes/ledger-rows.js`: the treasury files the page fetched + every month's summary and
  rows. Run with the slim file, move it away, run again: all seven months **identical** row for row
  (only the footer "Last updated" differs); the first run fetched only `treasury-recent.json`, the second
  the five full files. A slim file with a 30-day window → correctly fell back to the full files.
  `SLOW_DATA` still dashes (a35 probe).
- `data/treasury-recent.json` generated locally from the on-disk files and committed with this, so the
  live page uses it from the deploy; the hourly treasury step rewrites it from then on.

## Night — a12 (portfolio reflections queue), a26 (ErrorBoundary reset); a27 looked at, not shipped

Correction to the afternoon note: a26 and a27 are `index.html` items (ErrorBoundary; affiliate
commissions), not portfolio — only a12 is the portfolio one.

- **a12 `portfolio.html`** — index's three-endpoint `rpcFetch` pool replaces the single endpoint
  (transport failover; JSON-RPC errors still returned as-is so `getLogsRange` can bisect). The UFO
  Transfer-log reconstruction is no longer inside `fetchPortfolioData`: `fetchWalletData` loads the
  balances (all wallets in parallel, cheap eth_calls) and then queues the wallet's scan on
  `reflQueue`, which runs **one wallet at a time**. State per wallet: `ufo.reflections` `undefined` =
  queued/running, `null` + `ufo.reflError` = the chain read failed, number = done; `ufo.balanceWei`
  (string) carries the exact balance to the queue; `gen` per wallet (`genRef`) means a Refresh or a
  removed wallet can never be overwritten by an older load or scan (`dropReflScans` also empties the
  queue on Refresh / remove). `loadingAddrs` is a set (was one address, so the chips lied when several
  wallets loaded together). The aggregate's total is a number ONLY when every checked wallet has
  resolved — `reflState` ok | pending | failed with `reflPending` / `reflFailed` counts; a sum over
  three of five wallets is not shown as the portfolio's reflections. UI: tile "scanning N wallets…"
  / "unavailable" + **Retry** (re-queues only the failed wallets — `retryFailedReflScans`), lifetime
  bar "scanning the chain…" / "unavailable — the chain read failed for N wallets", per-wallet box
  "queued" / "scanning…" / "unavailable" (reflError in the title). "calculating soon" is gone.
- **a26 `index.html`** — `ErrorBoundary` takes `resetKey`; `componentDidUpdate` clears
  `state.error` when it changes. The dashboard boundary passes `${token}|${tab}`, so browser Back /
  a header link to the other token lands on a live page (before, the card followed the visitor until
  Home). A **Try again** button (clear the error, re-render the same children) joins Reload / Home.
- **a27 — looked at, left open.** Live API read from the browser pane (the VM proxy blocks
  ptgcapi): 169 entries over 2026-01…09, 114 paid / 55 unpaid / **0 partial** — the operator pays
  whole entries. Buys carry `paid`, `paidAtRate`, `paidCoverFee`, `pendingAmount` but no per-buy
  commission; a per-buy split would be Σ paid buys × `paidAtRate ?? entry rate` with the entry's
  `feeCovered` — five money surfaces to change for a state that does not occur today. Shaka's call;
  if wanted, the model is written up here and `getEntryStatus` already computes 'partial'.
- **Harness** — `HEAD_BLOCK=n` (raise the stub head past UFO's launch block or the scan is a
  no-op), `LOGS_DOWN=1` (eth_getLogs → JSON-RPC error, eth_call fine), `probes/portfolio-state.js`
  (chips / tile / bar / boxes), `probes/eb-state.js`. Runs: 3 wallets `SLOW=2000` — chips white →
  yellow → green, bar "scanning the chain…" for ~12 s, then the number; `LOGS_DOWN=1` — tile
  "unavailable" + Retry, bar names 2 failed wallets, Retry re-runs the scans (rpc count doubled);
  a26 with a `throw` gated on `window.__boom===token`: error on #/ufo/kpi → `#/ptgc` live →
  `#/ufo` card again (still broken, honest) → `__boom` off + Try again → UFO live. No console errors.
