# 2026-09-25 — New Home screen ("cosmic"), classic kept as the backup

## What Shaka asked for
Redesign the landing page. Mockups went back and forth in a Claude design canvas ("Grays Dashboard
Redesign" in Shaka's artifact gallery): A refined / B mission control / C mobile, then Shaka's own
AI reference image (gold + green alien over a two-coloured landscape), rebuilt as "D — Cosmic", then
a site-ready desktop + phone version. Two decisions from Shaka on the way:
- **No separate SELL button.** One button per card, **"BUY / SELL PTGC"** / **"BUY / SELL UFO"**, which
  opens the existing switch.win `SwapModal` (buy or sell is picked inside the widget).
- **Keep the old design as a backup** so he can go back to it.

## What shipped (index.html + two images)
- `HOME_DESIGN` (next to `UFO_MAINTENANCE`): `'cosmic'` now, `'classic'` = the design before today.
  `homeDesign()` also honours `?home=classic` / `?home=cosmic` in the URL, so either can be looked at on
  the live site without a deploy (e.g. `https://ptgc-ufo.com/?home=classic`).
- `Home` is unchanged except for one early `return` when the design is cosmic. All state, loaders,
  the a62 per-card paint, the a51 retry ladder, the 5-minute refresh, `SwapModal`, the maintenance
  notice and the secret dot are shared; only the view differs. The classic JSX below it is untouched.
- New module-scope components just above `Home`: `HomeCosmicView`, `CosmicCard`, `CosmicCardSkeleton`,
  `HomeSpark` (+ `fetchHomeSpark`, `homeSparkFallback`, `HomeSparkFlat`, `HcSk`). Styles: the `hc-*`
  block at the end of the `<style>` head (plain CSS, not Tailwind classes, so the harness CSS build and
  the play CDN cannot disagree about them).
- Art: `logos/home/cosmic-bg.jpg` (Shaka's image with its baked-in title and cards painted out and the
  card area darkened into a soft vignette, 1536x1024, 204 KB) and `logos/home/cosmic-ground.jpg` (the
  landscape strip for the foot of the phone layout). They live under `logos/` because `deploy.yml`'s
  `_site` allow-list copies `logos/` whole — a new top-level folder would NOT be published (a38).
- Layout: `--bgw` = clamp(1000px, 100vw, 1920px) is the painted width of the art; the title's top
  (0.172 x) and the card band follow it, so the cards sit on the dark part of the art at every desktop
  width. Two cards side by side from 1024px, stacked below; below 1024px the art fades out under the
  title and the ground strip returns at the bottom. Past 1920px the art stays 1920 wide and fades into
  the page colour at its sides.
- Card contents = the classic card's: price (`Price`, so the a48 sr-only full decimal is kept), 24 h
  change, contract + copy, Day counter, MCap (abbreviated below 640px), 24 h volume, PLS ratio, the
  chart, BUY / SELL, and "DASHBOARD ›" (a real button; the whole card still opens the dashboard).
  Null data prints "—" everywhere (gotcha 20); the change chip is "—", not a green 0.00%.
- **The chart is real data.** GeckoTerminal 15-minute closes for the token's `mainPair` with
  `token=<address>` (verified in a browser 2026-09-25: PTGC side, ~$0.0000388). GeckoTerminal skips
  intervals with no trades, so a quiet token has fewer points — drawn where they fall, never filled in.
  The series must end within 25% of the card's DexScreener price or it is not used. Fallback: the
  classic sparkline's five back-computed points; with neither, a flat dashed line. One request per
  card, cached 10 minutes. charts.html already depends on the same API.

## Checked (cloud harness, pinned Babel + Chromium)
- `npm run compile` clean. Home at 390, 1024, 1440, 1920 and at a 20px root font; `?home=classic`
  renders the old page exactly as before.
- `SLOW=9000`: the skeleton cards are the loaded cards' height (bottom edge 868 vs 867 px at 1440).
- `DS_DOWN=1 RPC_DOWN=1`: every figure "—", chips "—", flat dashed chart, no zeros.
- Copy button does not navigate; BUY / SELL opens `SwapModal`; "DASHBOARD ›" goes to `#/ufo`.
- `DS_PRICE=3.985e-5`: the $0.0₄3985 notation fits at 1024px.
- New harness env: `GT_CANDLES=1` (synthetic 15-minute candles; default is a 404 so the fallback path
  is what every older run sees), `GT_CANDLES=slow:<ms>`.

## Owed
- A live look after deploy on a phone and a desktop: art loads, both charts draw (if a chart is a
  smooth five-point curve instead of a jagged line, GeckoTerminal did not answer from that browser —
  an ad blocker is the usual reason; the card is still honest).
- To go back: `HOME_DESIGN='classic'` in index.html, commit, push. Nothing else.

## Round 2 (same day) — fills any window shape; headroom; buttons; stat tiles
Shaka's screenshot (a 1498x1311 window) had a black band under the art: the picture was 3:2, pinned
to the top at window width, so a window taller than 3:2 ran out of picture. Also: the alien's crown
was a hard crop in the source image; the BUY / SELL buttons were flat; the three stats had divider
lines he did not like.
- **Cover.** `--bgw = max(clamp(1000px,100vw,2560px), 100vh * 1.3196)` — the art is as wide as the
  window OR as wide as the window's height needs, whichever is more; a tall window scales it up and
  crops the sides. Never stretched. The landscape strip is a second background layer pinned to the
  page's foot for the shapes where content still runs past the art (short windows that scroll).
- **Headroom.** `cosmic-bg.jpg` is now 1536x1164: 140px added above, the top edge extruded upward and
  faded to black so the crown dissolves into shadow instead of ending on a line (the source image has
  nothing above it — a regenerated source with the whole head in frame would be the real fix; swap the
  file and drop the 140px band and the two fractions change back to 0.172 / 0.667). `--oy` shifts the
  art up by up to that band when the window is too short to show it: a 16:9 laptop is pixel-for-pixel
  what round 1 shipped; a tall window shows the fade-out and the cards sit further down. Phones hide
  the band always. Every vertical position is `fraction * --bgw + --oy`; the title sizes follow
  `--bgw` too, so the title stays proportional to the alien whatever drove the art's size.
- **Cards grow on tall windows** (chart `max(7.5vw,10vh)` up to 160px, card gap `1.6vh`, grid max-width
  `max(1300px,110vh)`).
- **BUY / SELL** got the classic rail's motion — `hcGlow` breathing box-shadow in the token colour and
  `hcSweep`, a light band crossing every 3 s (`::after`) — plus the token logo, Orbitron, an inner
  highlight. All animation off under reduced motion; the skeleton's button has none.
- **Stats** are three tiles (translucent fill, no dividers), label in the token colour.
Checked at 390x844, 1280x640 (scrolls; cards below the fold as intended), 1440x900 (same as round 1),
1498x1311 (Shaka's shape: art fills, footer at the foot), 1920x1080, 2560x1440; `?home=classic` untouched.
