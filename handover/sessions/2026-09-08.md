# Session — 2026-09-08

Participants: Shaka, Claude. Started from a full read of `index.html` (11,566 lines) and a live
check of ptgc-ufo.com on desktop and a 375 px phone.

## Delivered

### Review + roadmap
- Five-section code review (data layer, history/modals, Home/KPI/share, Dashboard render,
  affiliates/routing) → 58-item backlog in five phases, published as a shared tick-off page.
- Live findings that day: UFO burn tiles read "$0" for ~20 s; header stats clipped at ~1,100 px;
  mobile nav overflowed with no cue; Tailwind/Babel production warnings on every load.

### Phase 0 (17 fixes) — commit "Update index.html" 10:33
- Burn refresh guard compared a nonexistent field → burn stats never refreshed after boot.
- `fetchBurn` returns null on RPC failure (was zeros); headline shows "—" until read.
- `burnPeriods`: undefined/null/object semantics; tiles show Loading / — / value.
- Fee-timeline fallback scans from launch (was the 90-day window) — Oct 6 bug #1.
- Lifetime "pTGC burned by UFO" = window + checkpointed pre-window scan — Oct 6 bug #2.
- ErrorBoundary around Home / Dashboard / Affiliates.
- `load()`: cancel guards on every setter, try/catch, error screen with Retry/Back, other-token
  caches reset on switch, duplicate burn-period scan removed, in-flight dedupe in the fetcher.
- React mounts immediately; data fetches await `bootReady` (≤5 s) instead of blocking paint.
- Home: only stamps "Updated" on success; boot failure shows "refresh failed".
- Affiliates: leaderboard fallback (`Number(entry[valueKey])||0`), Buyer Distribution memo +
  abort + honest empty-state.
- Live Feed: replay timers cleared on re-run; NaN head → "retrying" + backoff.
- Head: preconnect, SRI on React/ReactDOM/Babel/Chart.js (chart.umd.js, not the on-the-fly
  minified name), html2canvas removed (never called).
- Header stats inline only from xl; KPI tile values smaller at lg.

### Pass 2 — commit "Update index.html" 11:03
- 641 lines of dead code removed (six unreachable modals, unused helpers/state).
- `burn-summary.json` staleness: label after 6 h, "—" after 7 d; warn once.
- KPI compare card and Combined Burn card were quoting the OLD UFO contract's burn windows;
  both now read the live contract from chain (Combined card shows the card first, windows land after).
- `getHeadBlock()` shared promise; in-flight dedupe for `getBlockTs` / `getPeriodBoundaries`;
  `putBucketed` evicts stale cache buckets; `dsPairsFor` hedged (documented endpoint first).
- Measured on a cold UFO visit: block-timestamp lookups 219 → 59, eth_blockNumber 5 → 1,
  DexScreener 14 → 6. eth_getLogs unchanged (~1,000) — that is roadmap d1.

### Pass 3 — in repo, not yet pushed
- `scripts/fetch-ufo-ptgc-burns.js` v3: scans BOTH UFO contracts (v1 retired, v2 live), tags
  rows with `c`, outputs combined + `byContract` totals, `schema: 2`, `liveContract`. Also
  remembers UFO-burn txs with no pTGC burn (`_noPtgcTxCache`) — ~160 receipts were being
  re-fetched every run. Dry-run against the real cache file with a mocked RPC: incremental
  path OK, second run makes 0 receipt calls.
- `index.html`: reads `byContract.v1` as the historical base (so the combined total is not
  double-counted), ignores non-v1 rows in the period math, and uses `byContract[live]` as a
  fallback lifetime source when the chain pre-window read fails.
- The "PTGC Burned by UFO" period boxes (24H/7D/30D/90D) now include the retired contract's
  burns for the whole window (they were cut off at the v2 launch). Shaka's intent, stated
  explicitly: the panel is old UFO + new UFO, always.
- This `handover/` folder.

## Verification method used
Exact-Babel compile in Node → headless Chromium mount with stubbed APIs and real `data/*.json`
→ walk all views → RPC-down / DexScreener-down / both-down runs → independent diff review
(two rounds, 8 + 8 findings, all fixed before delivery).

## Open questions / decisions
- Phase 1 build step deferred by Shaka ("not yet").
- Hosting is GitHub Pages; a Vite build will need a Pages-from-`dist` decision.
- The generator's v1 numbers include pTGC's own 0.5% burn-fee leg (always did); the chain scan
  for v2 excludes it. Small (0.53%) and documented in `computePtgcBurnedByUfo`.
