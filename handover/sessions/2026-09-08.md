# Session — 2026-09-08

Participants: Shaka, Claude. Started from a full read of `index.html` (11,566 lines) and a live
check of ptgc-ufo.com on desktop and a 375 px phone.

## Delivered

### Review + roadmap
- Five-section code review (data layer, history/modals, Home/KPI/share, Dashboard render,
  affiliates/routing) → 58-item backlog in five phases, published as a shared tick-off page.
- Live findings that day: UFO burn tiles read "$0" for ~20 s; header stats clipped at ~1,100 px;
  mobile nav overflowed with no cue; Tailwind/Babel production warnings on every load.

### Phase 0 (17 fixes) — commit "Update index.html" 10:33
- Burn refresh guard compared a nonexistent field → burn stats never refreshed after boot.
- `fetchBurn` returns null on RPC failure (was zeros); headline shows "—" until read.
- `burnPeriods`: undefined/null/object semantics; tiles show Loading / — / value.
- Fee-timeline fallback scans from launch (was the 90-day window) — Oct 6 bug #1.
- Lifetime "pTGC burned by UFO" = window + checkpointed pre-window scan — Oct 6 bug #2.
- ErrorBoundary around Home / Dashboard / Affiliates.
- `load()`: cancel guards on every setter, try/catch, error screen with Retry/Back, other-token
  caches reset on switch, duplicate burn-period scan removed, in-flight dedupe in the fetcher.
- React mounts immediately; data fetches await `bootReady` (≤5 s) instead of blocking paint.
- Home: only stamps "Updated" on success; boot failure shows "refresh failed".
- Affiliates: leaderboard fallback (`Number(entry[valueKey])||0`), Buyer Distribution memo +
  abort + honest empty-state.
- Live Feed: replay timers cleared on re-run; NaN head → "retrying" + backoff.
- Head: preconnect, SRI on React/ReactDOM/Babel/Chart.js (chart.umd.js, not the on-the-fly
  minified name), html2canvas removed (never called).
- Header stats inline only from xl; KPI tile values smaller at lg.

### Pass 2 — commit "Update index.html" 11:03
- 641 lines of dead code removed (six unreachable modals, unused helpers/state).
- `burn-summary.json` staleness: label after 6 h, "—" after 7 d; warn once.
- KPI compare card and Combined Burn card were quoting the OLD UFO contract's burn windows;
  both now read the live contract from chain (Combined card shows the card first, windows land after).
- `getHeadBlock()` shared promise; in-flight dedupe for `getBlockTs` / `getPeriodBoundaries`;
  `putBucketed` evicts stale cache buckets; `dsPairsFor` hedged (documented endpoint first).
- Measured on a cold UFO visit: block-timestamp lookups 219 → 59, eth_blockNumber 5 → 1,
  DexScreener 14 → 6. eth_getLogs unchanged (~1,000) — that is roadmap d1.

### Pass 3 — in repo, not yet pushed
- `scripts/fetch-ufo-ptgc-burns.js` v3: scans BOTH UFO contracts (v1 retired, v2 live), tags
  rows with `c`, outputs combined + `byContract` totals, `schema: 2`, `liveContract`. Also
  remembers UFO-burn txs with no pTGC burn (`_noPtgcTxCache`) — ~160 receipts were being
  re-fetched every run. Dry-run against the real cache file with a mocked RPC: incremental
  path OK, second run makes 0 receipt calls.
- `index.html`: reads `byContract.v1` as the historical base (so the combined total is not
  double-counted), ignores non-v1 rows in the period math, and uses `byContract[live]` as a
  fallback lifetime source when the chain pre-window read fails.
- The "PTGC Burned by UFO" period boxes (24H/7D/30D/90D) now include the retired contract's
  burns for the whole window (they were cut off at the v2 launch). Shaka's intent, stated
  explicitly: the panel is old UFO + new UFO, always.
- This `handover/` folder.

### Pass 4 — roadmap d1, in repo, not yet pushed
- `build-value-generated.mjs`: ports the launch-block fee-timeline fix, adds `fetchBurnPeriodsOnChain`
  (→ `burnPeriods.UFO`) and the lifetime pTGC pre-window scan with a checkpoint carried in the
  file (→ `delivered.ptgcBurnedAll/ptgcBurnedAllOk/ptgcPreWindow`), `meta.schema: 2`.
- `index.html`: one shared snapshot download per 5 min (`fetchValueGenSnapshot`); UFO burn tiles
  come from the snapshot when it is <3 h old (chain scan only as fallback), with an "as of" label
  after 1 h.
- Bug found by the dry run: `getLogsChunked` sent the last chunk of a historical slice as
  `toBlock:'latest'`, extending it to the present. Would have double-counted the lifetime figure
  after day 90. Fixed in both files (`exactTo` flag).
- Measured (stubbed chain, fresh snapshot): UFO visit 29 RPC calls, 0 `eth_getLogs`.
- Pushed; "Build Value Generated" ran green at 18:48 UTC; live UFO dashboard confirmed serving
  burn tiles from the snapshot with no Loading phase.

### Pass 5 — roadmap u2 share cards, in repo, not yet pushed
- New `ShareCardModal` shell: width-first scaling with a resize listener (the old formula used
  the larger viewport side as the width budget, clipping cards on phones), role=dialog, Escape,
  focus in/restore, body scroll lock, portalled to body, toolbar with Download PNG / Share / ✕.
- PNG export: html2canvas 1.4.1 loaded lazily on first click (SRI-pinned), rendered from the
  unscaled card at 2× via `onclone` transform reset; Web Share on phones that support files.
- All 13 cards converted (burn, value gen, allocation, DAO/PTGC-burned, targets, combined burn,
  combined value gen, RH cores, holders, Grays & Cores, affiliate, Combined Leagues, KPI report).
  Card content untouched except the Leagues card's dashed "screenshot here" border, now gone.
- Verified headless with a static Tailwind build + local html2canvas: every card opens at 1280
  and 390 px, fits the viewport, closes on Escape, and downloads a PNG of the right size
  (1200×628 … 2400×1350); Chart.js canvases render in the export.

### Pass 6 — Phase 2 leftovers, in repo, not yet pushed
- d9: cached 7D/30D/90D volume windows are no longer discarded when today's live 24h volume
  exceeds them (a spike blanked the Value Generated panel); guards are now file age <24h +
  monotonic series + token-age.
- d8: when DexScreener is down and price comes from the chain, market cap = remembered implied
  supply × price (persisted in localStorage), falling back to starting supply minus known burn.
- d11: holder-tier scan takes an AbortSignal (aborted on token switch/unmount), reports the
  20-page cap as a lower bound instead of exact, de-duplicates LP addresses.
- d5: per-address log-scan errors (`logErrorByAddr`) replace the shared `lastLogError` global.
- d7: `ADDR` / `SUPPLY` canonical blocks; all duplicated address/supply literals alias them;
  `resolveUfoPairs` partners derived from `HARDCODED_UFO_PAIRS`; one `CORE_LOGOS` map.
- Reviewed (6 findings, all fixed): cold-cache mcap now subtracts known burn; abort/timeout stay
  armed through the response body; cancelled scans return quietly.

### Pass 7 — roadmap u7 + u8 clarity/mobile pass, in repo, not yet pushed
Scope set by Shaka: **no changes to the creature emojis and no tier labels under them.**
Everything else in u7/u8 done.
- u7 `InfoTip`: a 32 px ⓘ button that opens a small card on tap/click (Escape, outside tap,
  scroll or resize closes it; `role=dialog`, `aria-expanded`). Portalled to `<body>` and
  positioned `fixed` from the button's rect — the panels are `overflow-hidden` and the header
  has a backdrop-filter, so an absolute child would be clipped/mis-placed. Flips above the button
  when there is no room below. Copy lives in `EXPLAINERS` (one object, right after the component).
- Explainers wired: **X's to ATH** (quotes the ATH; UFO variant says the ATH is the original
  contract's on purpose), **X's to a Penny**, **PLS Ratio** (all three in both the xl row and the
  sub-xl fallback row), **Day N** (the whole pill is the tap target — a separate ⓘ widened the
  name block enough to push the price onto its own line at 375 px), **Value Generated** (panel
  title), **RH Cores** (LP Pairs switch, phone and desktop rows).
- Value Generated panel: one-line note under the header — "5% tax on every trade, split across the
  tiles below — estimated from volume" (PTGC) / "…measured on chain" (UFO, live rate from
  `cfg.taxRate`; wording falls back to "The tax on every trade" if the rate is 0/unknown).
- u8 nav overflow cue: `useScrollCue(ref)` + `ScrollCues` — a fade + ‹/› chevron on whichever edge
  has more content, on both the phone nav row and the desktop Row-2 nav. Shows only while the strip
  actually overflows (it does not appear at 1440 px).
- u8 orphan 7th KPI tile: Txns 24H spans the full row below lg (`col-span-2 sm:col-span-3
  lg:col-span-1`) with its content laid out as a strip.
- u8 header wraps: Burn / Value Generated / Token Allocation panel headers are `flex-wrap`, the
  title is `whitespace-nowrap` and the control group drops under it on phones (Value Generated's
  USD/TOK + window group takes a full-width row `w-full sm:w-auto`). "/ PTGC" suffixes no longer
  break at the space (was visible at 320 px).
- u8 tap targets: `.tap` (32×32 box, glyph unchanged, negative margins keep the layout) on every ⓘ,
  📷, ✕, the header 📋 and ← , the KPI-tile corner buttons (ⓘ / RH), the DAO breakdown toggles, the
  FreshnessBadge refresh icon; `.tap-h` (32 px tall) on USD/TOK, the 24H/7D/30D/90D group, the sort
  buttons, Leagues and Ledger. The RH switch is now a `role=switch` button with the 36×16 track
  inside a 32 px box. aria-labels on every icon-only button touched; `aria-pressed` on the toggles.
- `prefers-reduced-motion` now stops `.animate-fire` / `.animate-pulse-slow` too.
- `tools/harness/` — the headless harness from the handover, ported into the repo (README there).
  Verified this pass with it: PTGC + UFO at 375×812, 320×700 and 1440×900; Home; KPI Report,
  Socials, Live Feed tabs; every explainer opens and Escape closes it; RPC-down and DexScreener-down
  runs mount without a page error; a probe of every visible `button`/`a` finds nothing under 32 px
  tall (two text buttons are 30–31 px wide).

### Pass 8 — roadmap u12 keep the shell while loading, in repo, not yet pushed
- The dashboard's `if(loading) return <spinner>` is gone. Header and both nav rows render at once;
  price, change and the three stats show grey bars (`Sk`), the Day pill has a pill-sized
  placeholder (so the phone header is the same height before and after), the freshness badge
  says "Loading…" with the icon spinning, and a thin token-coloured sweep runs under the chrome.
- Tab bodies: `DashboardSkeleton` for the Dashboard and KPI tabs — the KPI strip skeleton reuses
  the real tiles' classes and line heights (measured: strip top/height within 3px on desktop,
  ≤10px on a phone), four `SkPanel`s at roughly panel height, an LP-section stub. Socials renders
  its real content while loading, so a token switch from a Socials card now keeps the page and
  opens the share modal over it instead of showing a blank "Loading…" page first.
- The failed-first-load screen ("Couldn't load PTGC" + Retry/Back) is unchanged; Retry now goes
  back to the skeleton shell rather than the spinner.
- Home: `HomeCardSkeleton` mirrors `Card`'s wrapper, gaps and element sizes. Measured against the
  real cards: 376 vs 374 (375w), 176 vs 176 (640w), 196 vs 196 (1440w) — the old `h-32` blocks were
  128px, so the page used to jump ~250px on a phone when data landed. A placeholder also holds the
  freshness-badge row.
- Harness: `SLOW=<ms>` env delays every RPC/DexScreener/PulseScan reply so the loading frame can be
  screenshotted and measured (`tools/harness/README.md`).

## Verification method used
Exact-Babel compile in Node → headless Chromium mount with stubbed APIs and real `data/*.json`
→ walk all views → RPC-down / DexScreener-down / both-down runs → independent diff review
(two rounds, 8 + 8 findings, all fixed before delivery).

## End-of-day state
- Live: Phase 0, dead code, snapshot pipeline (d1/d4), share cards (u2).
- Pushed 19:48: Phase 2 leftovers (d5, d7, d8, d9, d11) + handover notes.
- Pushed 22:35: u7/u8 clarity + mobile pass, `tools/harness/`.
- In repo, not yet pushed: u12 loading shell + Home skeletons, these notes.
- Roadmap 35/58. Next: u1 Modal wrapper + u9 a11y. Vite build still parked.

## Open questions / decisions
- Phase 1 build step deferred by Shaka ("not yet").
- Hosting is GitHub Pages; a Vite build will need a Pages-from-`dist` decision.
- The generator's v1 numbers include pTGC's own 0.5% burn-fee leg (always did); the chain scan
  for v2 excludes it. Small (0.53%) and documented in `computePtgcBurnedByUfo`.
- Decided today (pass 7): the creature emojis stay as they are — no text labels under them
  (u7's "tier labels" sub-item is won't-do; the rest of u7 is done).
- Decided today: UFO's ATH stays the original contract's (migration, same token). Affiliate
  commission rules are managed elsewhere — the dashboard only displays the API's figures.
