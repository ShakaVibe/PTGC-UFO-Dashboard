# Session 2026-09-14 — DAO Buys: Grays creatures + Recent-buys ledger (g8, still hidden)

Shaka's brief: under the chart, in its own box, a ledger of the last buys — preload the last
10 — with date, PTGC bought, PLS spent, dollar value at the price paid, and the sea-creature
emojis for what was bought ("3 dolphins, or 2 dolphins and one squid"). Also put the creature
line in the "PTGC bought" tile, centred under "211 buys lifetime".

## What changed (`index.html` only, +~90 lines; not yet committed at the time of writing)
- **`DaoCreatures`** (module scope, right after `DaoBuysStat`): a PTGC amount as Grays
  creatures. Reuses `getBurnC(pct)` — the same greedy change-making the burn tiles use
  (🐋 = 1 % of starting supply, 🦈 0.1 %, 🐬 0.01 %, 🦑 0.001 %, 🐢 0.0001 %, 🦐 0.00001 %),
  `.slice(0,3)` = top three tiers (same cut as the KPI page's lifetime-burn line). Counts ≤ 4
  are drawn as a row of creatures (`e.repeat(cnt)`), 5+ as `🦈×12`. `role="img"` with an
  aria-label in words ("2 Whales, 5 Dolphins, 9 Squids"). Below a shrimp → a single 🐚.
  Lifetime 6.87 B = 🐋🐋 🐬×5 🦑×9 (checked by hand: 2.0599 % of supply).
- **`DaoBuysStat`** gained an optional `foot` prop (centred flex row under `sub`, may wrap).
  The "PTGC bought" tile passes `<DaoCreatures ptgc={view.t.ptgc} size="text-lg lg:text-base"/>`
  — one line in the 7-column desktop grid, larger in the 2-column phone grid. Skeleton tile 0
  got a matching extra `Sk` line (gotcha 11).
- **Recent-buys ledger**: new box after the chart/cadence container, before the footer, same
  `mx-3 sm:mx-6 rounded-2xl bg-[#0b0b0b]` shell as the chart. `ledgerN` state (10, +10 per
  "Show 10 more"); rows = `view.buysAll` (LIFETIME, newest first — deliberately not the
  selected range; fresh on-chain buys are included and carry the NEW pill). Value = `b.usd`
  (what was paid at the block's PLS/USD). Date links to the PulseScan tx.
  - sm+: `<table>` Date · time ↗ | PTGC bought | PLS spent | Value | Grays (Grays th is
    `w-full` so the numeric columns cluster left).
  - phones (`sm:hidden`): a `<ul>` with two lines per buy — date · time ↗ / `43.88M PTGC`,
    then `200.00M PLS ($2,224)` / creatures right-aligned. The 5-column table was too
    cramped at 375 px (creatures wrapped to two rows).
  - Header row: "RECENT BUYS" + "10 of 211".
- DAO Buys `InfoTip`: one more sentence explaining the Grays tiers.
- Share card untouched.

## Verified (harness)
The local VM has no Chromium and the Playwright download is blocked by egress, so the harness
ran in Claude's cloud workspace: `tools/harness/out/harness-bundle.tgz` (gitignored — index,
pngs, harness files, the small `data/*.json`) staged up, `npm install` there, `CHROME=` the
preinstalled Playwright Chromium. Runs: 1440×900 modal + scrolled ledger, `Show 10 more`
click (20 rows, no console errors), 375×812 stats + ledger. `md5sum index.html` identical on
both sides afterwards. Screenshots sent in chat; the container's emoji font is not Apple's,
so the creatures look different there than on the Mac/phone.

Leftovers in `tools/harness/out/` (gitignored, Claude cannot delete from the bridge):
`harness-bundle.tgz`, `patch2.py`, `patch3.py` — safe to `rm`.

## Open for Shaka
- Review in a real browser (header dot on the live site after pushing, still hidden behind
  `DAO_BUYS_LIVE=false`). Things easy to flip if wanted: the ≤4 repeat / 5+ `×N` split
  (`DaoCreatures`), three tiers deep (`.slice(0,3)`), lifetime vs selected-range rows
  (`view.buysAll` → `view.buys`), 10 per page.
- Creature line on the share card? Not added.

## Review round 1 (Shaka, from the live-ish screenshot)
1. Creatures are always `🐋 ×2`, never `🐋🐋` — `DaoCreatures` lost the ≤4-repeat branch;
   `×1` is shown too, like the site's other `x{cnt}` rows.
2. "PTGC bought" tile stops at Squid (`min="Squid" max={8}` → `getBurnC(pct,'Squid')`): lifetime
   = 🐋 ×2 🐬 ×5 🦑 ×9. Per-buy rows keep the default (all tiers, three deep).
3. Latest-buy tile: `<DaoCreatures>` after "PTGC" on the amount line (`text-base sm:text-lg`).
   Sits inline on phones (2-col grid); in the 7-col desktop grid the tile is ~300 px, so the
   creatures wrap to a line directly under the amount — the tile grows, nothing clips.
   Also appears on the share card (same component, `compact`).
4. Ledger → collapsed bar: "RECENT BUYS … 211 buys ▼" full-width button (`aria-expanded`,
   `aria-controls="dao-buys-ledger"`) in the same box shell under the chart; click rolls it out
   (`grid-template-rows 0fr→1fr` .35 s, rows stay mounted, `inert` + `aria-hidden` while
   closed). Opens with the last 6 (`ledgerN=6`), "Show 10 more" adds 10. Type up a step
   (table `text-base`, headers `text-xs`, phone rows 15 px), PTGC bought is the full number
   (`fmt`), new **At ATH** column = `b.ptgc × athPrice` (the modal's `athPrice` prop; "—" if
   0). Phones: three lines per buy — date + PTGC / `PLS ($) · at ATH $` / creatures.
   Header button uses `tap-h`, not `tap` (`.tap` centres its content).
Harness runs (cloud): desktop closed/open/+10, phone stats + open. No console errors.

## Review round 2
1. Stat tiles top-aligned (`DaoBuysStat` `justify-start`, was `justify-center`) so 6.87B sits
   on the same row as the other big numbers even though its tile has the creature line.
2. Latest-buy tile: creatures on the amount line, right of "PTGC", `flex-nowrap` — never under.
   `min="Squid"` and `ones={false}` (a lone creature has no ×1 — Latest tile ONLY; Shaka
   said the ledger keeps its ×1). To fit the ~290 px tile in the 7-col grid at ~1150 px, the
   amount is `text-3xl` up to xl (was 4xl from sm) and the creatures `text-base xl:text-lg`;
   logo 40 px on sm+. Measured at 1156 and 1440. A 5-dolphin buy (`🐬 ×5 🦑 ×2`) is the
   widest realistic case and still fits at 1156.
3. Recent-buys box: gold outline + glow (`border-[#D4AF37]/45`, box-shadow like the Latest
   tile but softer), the bar itself gold-shaded (90° gradient .22 → .03) with the PTGC logo
   left of "RECENT BUYS" (title in `#F5DE83`); "Show 10 more" is a gold-outlined pill, centred.
4. Type up another step on sm+: table `text-lg`, headers `text-sm`, creatures `text-2xl`,
   `px-5 lg:px-6` cells, `py-3` rows. The table is now auto-width and `mx-auto` (centred in the
   box) instead of `w-full` with the Grays column soaking up the slack.
Phone list unchanged apart from the shared bar styling.

## Review round 3
Tiles back to `justify-center` (Shaka: top-justified "does not look good"; the first tile's
creature line sitting lower than the neighbours is accepted). Ledger column "Grays" → "Positions".
Round 3b: the "PTGC bought" title still sat higher than its neighbours (the creature line
shifted the centred block up). `DaoBuysStat` now renders an invisible copy of `foot` above the
label, so the label/number/sub centre exactly like the other tiles and the creatures hang below.
Round 4: bar title "RECENT BUYS" → "RECENT DAO BUYS".

## Phone header: Buy/Sell row (Shaka's pick "A")
On phones the Buy/Sell + Switch pair `ml-auto`'d onto a second wrap line under the price,
leaving a hole under the logo ("looks terrible"). Three mockups at 375 px (`tools/harness/out/
index.mock{A,B,C}.html`, gitignored; `mock.py` makes them): A = full-width row, Buy/Sell and
Switch half-and-half under the identity block; B = one full-width Buy/Sell bar + Switch chip
under the price; C = Buy pinned in the sticky tab bar + Switch chip. Shaka took **A**. Only
phone classes changed (`basis-full mt-3 grid grid-cols-2` below sm, `justify-center w-full`
on both buttons); sm+ is byte-for-byte the old layout. B/C stay in `mock.py` if wanted later.

## Share-card PNG ("looks like shit")
Shaka's downloaded DAO Buys PNG: every metallic-gold word a solid gold bar, tile text cut in half,
"4.6d ago" hanging out of its pill. Reproduced in the cloud harness by serving the real
html2canvas 1.4.1 (`H2C=1`, now a devDependency) and pressing the actual Download button
(`probes/share-png-real.js` intercepts the blob). Causes and fixes, all in `shareCardPng`'s
`onclone` (so every card gets them): `background-clip:text` unsupported → `.metallic-gold`
flattened to `#E8C044`; text in `white-space:nowrap` / `truncate` drawn ~half a line low and
clipped → nowrap off + overflow visible inside `[data-share-wrap]`. The "ago" pill's border box
still painted ~12 px above its text whatever I tried (display, line-height, weight, radius,
colour), so `DaoBuysLatest compact` renders the age as plain text. Burn-panel card re-checked
through the same path: unchanged. Result matches the screen except the gold is flat, not
metallic — Shaka said take the button away if it can't match; this is close enough to keep.
**Decision:** Shaka: flat gold is not good enough — "take the buttons away, we will just
screenshot". `ShareCardModal` gained a `screenshot` prop (no Download PNG / Share buttons, footer
says to screenshot); the DAO Buys card passes it, the other cards keep their buttons. The
html2canvas `onclone` fixes stay in place for them. The "ago" pill on the card is a pill again.

## LIVE
Shaka: "make it live". `DAO_BUYS_LIVE` const and gate removed, header preview dot removed, the
panel button renamed **PTGC Buys** (left of Ledger, `aria-label="PTGC Buys — DAO Buys chart"`).
Harness README entrance updated to `button[aria-label^='PTGC Buys']`.

## Second DAO wallet (evening)
Shaka: 0x440773B5104a102c00EF26979a5c897155336A34 also bought PTGC for the DAO. It was already
`WALLET2` in `fetch-treasury-transactions.js` (hourly `treasury-wallet2-*.json`), just not in the
buys generator. `build-dao-buys.mjs` now walks `WALLETS` (both), tags each buy with `wallet`,
writes `wallets` + per-wallet `source` (schema 2), extends the daily price line BACKWARDS on the
same grid when the first buy moves earlier, and skips grid points where a pair didn't exist yet
(the PTGC pair appears 2023-10-12). Rebuilt locally in 10.8 s (`NODE_USE_ENV_PROXY=1` — Node's
fetch can't resolve DNS through the VM proxy otherwise): 317 buys (211 + 106), 19.21B PTGC for
66.62B PLS ($1.88M), first buy 2023-10-28, daily line from 2023-10-12. Wallet 2 excluded 31 LP
removals + 14 no-PLS txs. Wallet 2 paid ≈ market (0.97–1.06×, median 0.995) — no 5 % tax on it,
unlike wallet 1's 3–14 % premium; worth knowing if anyone asks why the avg-cost line moved.
Browser: `ADDR.DAO_WALLET2`; `fetchFreshDaoBuys` scans all `file.wallets`; hover card shows the
wallet; InfoTip copy mentions both. Harness: modal, hover (biggest dot = wallet 2's 1.41B buy),
ledger — no errors. Hourly runs continue incrementally (prices cached by hash).
Later: "NEW" pill on the PTGC Buys button (same pill as the Live Feed nav badge). Remove when it stops being new.

## Buy/Sell security audit
See `handover/security-buy-sell-2026-09-14.md`. Footer copy + iframe `allow`/referrer tightened.

## GitHub account hardening (end of day)
Shaka enabled 2FA on GitHub — SMS only so far. Still to do (5 min, from
`handover/security-buy-sell-2026-09-14.md`): save recovery codes, add a passkey (Touch ID /
iPhone) then REMOVE SMS, check Settings → Collaborators (should be just Shaka) and revoke stale
tokens / OAuth apps, `main` ruleset (block force-push + deletion, no PR requirement), Pages
"Enforce HTTPS" + verify the domain, then the registrar: 2FA + transfer lock.

## "Tax" → "Fee" wording sweep (night)
Shaka: the word "tax" must not appear anywhere on the site. Removed the u7 one-liner under the
Value Generated header ("5% tax on every trade, split across the tiles below …") — the ⓘ explainer
still says it, now as "fee". Then a whole-file sweep of `index.html`, `calculators.html`,
`portfolio.html` and the two generators: visible copy ("Fee distribution breakdown", "% fee rate",
"the 5% fee and slippage", "fee-paying transfers"; the disclaimers now read "financial, investment,
or legal advice"), console lines ("FEE BYPASS"), comments, AND identifiers — `taxRate` → `feeRate`,
`taxBreakdown` → `feeBreakdown`, `st.tax` → `st.fee`, `taxedLegs`/`untaxedLegs`… → `feePaid…`/
`feeFree…`, `untaxedBy` → `feeFreeBy`. No `data/*.json` or harness file carried a `tax` key, so
nothing external changes. Verified: `npm run compile` (index) + Babel transform of the other two
pages + `node --check` on the generators all pass; `grep -i tax` on the four files finds only
`syntax`/`fmtUnitAxis`. Not touched: `tools/harness/out/*.html` snapshots and old handover notes.
Also: the Value Generated `<h3>` had `lg:text-xs xl:text-sm` + tighter lg tracking (squeezed to
fit the toggles at 1024–1279px); Shaka: all four panel titles must match — now the same classes
as Total Supply Burned / Token Allocation / DAO Treasury. The header row is `flex-wrap`, so if it
ever gets tight the USD/TOK + window buttons drop under the title instead of shrinking it.
Also: the "/ PTGC" / "/ UFO" suffix in the four dashboard panel titles (Total Supply Burned, Value
Generated, Token Allocation, DAO Treasury) is now the token logo (`TOKENS[token].logo`, 20px /
24px from sm (`-my-1` keeps the title row height), `alt`+`title` = token name) — Shaka wanted the space back.

## u5 + u6 — one Value Generated model, share-card parity (night)
Worked in the cloud workspace (clone of `main` at `d5c4a70` + the harness; Chromium there),
written back through the bridge. `index.html` only.

**u5 — `computeValueGen` (module scope, right after `computeValueGenBuckets`).** One model,
built once per Dashboard render (`valueGenFor(period)` → `valueGenView` for the selected
window, `valueGen7d` for the KPI card) and printed by every surface: the panel headline
(`liveTotalUsd`) and its PTGC tiles, the Value Generated share card, the Combined card
(`FeeBlock` now takes `vg`), and `KPIContent` (prop `valueGen7d`, passed from all three
mounts — KPI tab, Socials KPI modal, Combined KPI). Answers carry a `basis`:
`delivered` (UFO measured) → `accrued` (delivery scan still running; only reflection known
per bucket) → PTGC `volume` (window volume × fee rate — its only basis); `pending:true` while
UFO's read is in flight (dash, never an estimate); `total:null` when there is nothing honest.
`valueGenBucketUsd(vg,item)` gives a tile's USD or null → "—". The old burn ÷ hard-coded
0.005/0.01 guess is gone. The KPI compare card (other token) uses `vol7dFromCache(t)` —
the same coingecko-data series the panel uses — with `estimate:true`; UFO seen from the PTGC
page is tagged **7D EST** because that page has no chain read for it. A console.warn fires if
the panel's per-tile sum ever drifts >0.5% from the model.
**Live bug found on the way:** the PTGC KPI Report's VALUE GEN tile read "—" forever (the
measured path only existed for UFO and `null` meant "still loading"). Now $9,559 = the panel.

**u6 — parity.** KPI card + TwitterCard: unknown 7D window / burn total → "—" (was 0 / $0),
creature slot dims to 🐚 when unknown. Allocation card tiers → `tierCountLabel` ("—", "≥N"
when the scan hit its cap), the panel uses the same helper. DAO card: "Pending in Live (n)"
and "Ready to Deploy" like the panel (the panel's own breakdown popover said "Pending in
Proposals" — fixed too); "—" while `daoData` is null (panel says "Loading DAO data…").
PTGC-burned-by-UFO card windows: "—" when unknown. X's/Targets card: market-cap multiples are
now target ÷ the market cap shown in the tile (were ÷ starting supply, so tile and multiples
disagreed by the burned share). Prices: `fmtPrice` (the header's) everywhere on cards —
`formatSubPrice`, `formatPriceTwitter`, `PriceEl` are thin wrappers; Socials hub's raw
`toFixed(6)` / `'0.000000'` → `<Price>`. `fmtTokenQty` prints "<0.000001" instead of "1.23e-7".

Harness: `#/ptgc` panel = KPI card = VG share card = $9,559 (7D, real coingecko file);
`#/ufo` panel/card/DAO card; `RPC_DOWN=1 #/ufo/kpi` + Add PTGC; `DS_DOWN=1 #/ptgc/kpi` (VALUE
GEN "—"); Socials → Combined Value Generated + Combined KPI twitter card; 375 px KPI. No
console errors. Note the stub chain gives UFO 0 fees, so UFO's Value Generated reads $0 with
no tiles in the harness — panel and card alike (parity), not a bug.
Live check by Shaka: 1–7 all good except the Combined Value Generated card — with UFO's six
buckets live the card outgrew its 600×540 frame and the total bar was clipped. Frame is now
600×610 (card `height:610px`, column `space-between` so the bar is pinned to the bottom even
when a block has no tiles), `FeeBlock` and the total bar ~10 px tighter. Measured in the
harness: header 32 + 2 × 209 blocks + bar 86 + gaps/padding = 598 ≤ 610.
