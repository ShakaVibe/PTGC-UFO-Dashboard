# 2026-09-16 — u14 versioned localStorage keys, u11 Live Feed render cost + edge cases

## Start of session
- Working tree clean, no `.git/*.lock`, HEAD `dc11e9f03` (the 09-15 commit). `index.html` md5
  identical to origin/main, so the work was done on a clone of `main` in the cloud workspace
  (Chromium there) and written back through the bridge: `index.html`, `tools/harness/run.js`,
  `tools/harness/README.md`, `handover/`.
- Pipeline check (from origin, since neither shell can reach ptgc-ufo.com): `data/dao-buys.json`
  `generatedAt` 2026-09-16T17:44Z, 318 buys (317 yesterday). The local clone was just behind —
  a `git pull --rebase` brings it up. Nothing to fix.
- Shaka picked "Phase 3 leftovers (u11 / u14)" for the session.

## u14 — localStorage keys versioned (`LS` table, right after `SUPPLY`)
Every key the page writes is now listed once in `LS`, each carrying a version, with `lsGet(key,
shapeOk)`, `lsSet`, `lsTake` (read-and-clear) and a boot-time `lsSweep()`:
- `grays_disclaimer_v1` holds the `DISCLAIMER_VERSION` the visitor accepted (`'1'`). **Bump
  `DISCLAIMER_VERSION` when the wording in `DisclaimerModal` changes** and every device is asked
  again — the old bare `ptgc_ufo_disclaimer_accepted='true'` is upgraded to `'1'` once, so the
  current wording is not re-prompted on deploy. Verified: missing key → modal; `'0'` → modal;
  accept → `'1'`, modal gone.
- `grays_tiers_v2_<address>` — the holder-tier cache is now shape-checked before use (`ts`
  number, `tiers.ok===true`, every tier a non-negative number or null). A string count or a
  missing tier is ignored, not rendered as "0 whales". Old `grays_tiers_0x…` entries are swept.
  Only `ok:true` results were ever cached, so the check matches the writer.
- `grays_ptgc_by_ufo_pre_v1`, `grays_implied_supply_v1`, `grays_deck_pools_v1` — unchanged
  names, now aliases of `LS.*` (they already validated their shape).
- **The `ptgc_last_token` / `ptgc_nav_tab` / `ptgc_nav_view` keys keep their names.** They are the
  handoff contract with `calculators.html`, `charts.html` and `portfolio.html` (those pages set
  them, index reads them once). Renaming would touch four pages for no gain; they are listed in
  `LS` as `HANDOFF_*` and read with `lsTake`.
- Legacy `?token=X` handoff: still `replaceState` (no duplicate history entry — verified
  `history.length` 2), but now only the `token` param is dropped; `?debug=1` and anything else
  survive (`?token=UFO&debug=1` → `/?debug=1#/ufo`).

## u11 — Live Feed (`AbductionDeckModal`)
Three module-scope components hoisted out of the deck, right after `DECK_POD_POS`:
- `DeckRow` (`React.memo`) — one ground-tape row. The deck re-renders on every landing (6×
  `setTotals` per contact) and every 6 s poll; forty rows × (4 + pods) cells were rebuilt each
  time. A contact object never changes identity, so a row now renders once. `.deck-a.on` is a
  DOM class the dispatch animation adds; React never overwrites it because the row never
  re-renders. **Reduced motion: cells start lit** (`r.fresh&&!reduced`) — they used to stay dimmed
  forever because the dispatch that lights them never runs.
- `DeckScanline` — owns the half-second clock for "next scan Ns"; `tickNow` in the deck (a
  full re-render twice a second) is gone. **Contacts 24h** is now `{base, at, landed}`: the
  DexScreener 24h count at seed time, aged out evenly over 24 h, plus contacts landed since. It
  used to only ever go up between dashboard refreshes.
- `DeckPods` — the pods and the ship's "Generated" readout, with the 1.1 s count-up inside
  (`disp`/`dispRef` moved here). It used to `setDisp` on the deck itself every animation frame.
  `podRefs` is still the deck's (passed down) so the dispatch flights and the signal lines find
  the pod elements.
Also: `.deck-scan` lost `mix-blend-mode:overlay` (a full-deck blend layer composited every frame
for an effect invisible on `#0b0d12`; alpha bumped .025→.03 to compensate). `deckPodPos(n)`
returns an arc layout for n>6 instead of throwing on `pos[6]`; `cols` omits `repeat(0,…)` (invalid
CSS) when there are no pods. Learned pools (`learnPool`): partner named from `ADDR` when known
(`DECK_KNOWN_SYMBOLS`), else `symbol()`, else its short address — never "pool / ?"; `symbol()`
is not even called for known partners.

Verified in the harness (new options, see README): `DECK_LOGS=8` gives the tape synthetic
swaps — 1440×900 PTGC: 3 rows + replays landing every 7 s, lift chip on the ship, top row
dimmed until dispatch, pods $27,751/$13,875/…, readout = sum, "Contacts 24h 200", countdown
ticking; `REDUCED=1` UFO: 8 rows at once, 0 dimmed cells, 0 chips; 375×812 phone deck; `RPC_DOWN=1`
→ "chain read failed"; Home, UFO KPI, Escape + KPI walk: no console errors. Acorn scope pass
over the three hoisted components: no free identifiers outside module scope.
Not a regression but worth knowing: in the harness the UFO deck has **0 pods** (the `eth_call`
stub returns zero fees, so every bucket is filtered out) — same on the previous build.

## Harness additions (`tools/harness/run.js`)
`DECK_LOGS=n` (n synthetic PulseX Swap events on the stub pair, newest at head), `REDUCED=1`
(`page.emulateMedia({reducedMotion:'reduce'})`), `NO_ACCEPT=1` (don't pre-accept the
disclaimer), `reload=<ms>` action. The pre-accept key is now `grays_disclaimer_v1`.

## Roadmap
u11 and u14 ticked on the artifact. 45 of 59 closed.

## Commit
`cd ~/Desktop/PTGC-UFO && git add -A && git commit -m "u11 Live Feed: memoised rows, own clocks for countdown and count-up, no full-deck blend layer, >6 pods, learned-pool names, Contacts 24h ages; u14 versioned localStorage keys (LS table, disclaimer version, tier-cache shape check, ?token= keeps the query); harness DECK_LOGS/REDUCED/NO_ACCEPT/reload; handover 2026-09-16" && git pull --rebase && git push`

---

## Evening — Audit II (whole-repo deep dive) → roadmap a1–a67

Shaka asked for a full deep-dive audit of the site and for the findings to go on the to-do list.

**Method.** Nine parallel reads on a clone of `main` at `0ef983c`: `index.html` in six slices, the four
sibling pages in two, every script + workflow in one; each briefed with the handover gotchas and the open
backlog so deliberate choices (UFO ATH, emojis, no "tax", parked build, u3, g1–g6) were not re-reported.
Every claim re-read at its line before it went in. ~150 raw findings → 67 roadmap items (`a1`–`a67`, new
"AUDIT II" group on the artifact, phase id `p5`). Raw findings with evidence and scenarios:
`sessions/2026-09-16-audit.md` (the a-items point at it).

**Live check** (browser pane, 1440/1280 px and 375×812): Home → PTGC → DAO Buys → Live Feed → UFO →
Charts → Calculators → Ledger → Portfolio. No console errors anywhere; the u11 deck lifts/lights/counts
down on the live site; cold PTGC = 40 RPC calls, UFO with a fresh snapshot = 193. Four things the code
reads did not catch, all now items: Charts "24H" is 48 h for PTGC/UFO vs 24 h for BTC/ETH (a1); the Ledger
shows the Sep 15 buy as 634.48K PTGC where the DAO Buys chart says 48.29M — the buy went through two pools
and `tokensIn.find()` takes the first transfer (a4); the Live Feed ✕ is at x=400–428 on a 375 px viewport
so a phone can't close it (a25 — pre-existing, not from the morning's u11 work); `$0.0₄4542` reads as
"$0.044542" to screen readers/clipboard because the zero-count is a `<sub>` (a48).

**Headlines (read a1–a38 on the artifact).** Wrong numbers: a1, a2 (Ledger burns from calldata, 307.0M vs
293.2M), a3 (`d90` is CoinGecko's 200-day change — confirmed in the data file), a4. Sibling pages never got
the Phase-0 hardening: no SRI anywhere, ledger's Babel unversioned (a13); portfolio can blank permanently
on a bad saved entry (a10) and cache PTGC balance 0 on an RPC miss (a11); calculators single RPC + zeros
(a6), token-switch race (a7), Rewards save effects declared before their state (a8). Pipeline: the hourly
workflows land every 4–5 h — verified from commit stamps, value-generated 18:08 / 21:18 / 00:09 / 04:45 /
09:51 / 14:49 / 18:51 (a29); PulseScan pages cap at 250 and the treasury fetcher stops after page 1 (a28);
`ptgc-burns-2026.json` hits 100 MB ~mid-2027 (a30); failed getLogs chunks silently dropped (a31);
`handover/` is public and deployed (a38 — this file included). index.html's remaining silent zeros: the
DexScreener-outage fallback (a14), burn USD / allocation donut (a15), DAO panel (a16), PTGC-burned-by-UFO
headline (a17), five share cards (a18), KPI compare card (a19), quickRefresh token guard (a20), UFO LP
prices (a21), deck-reopen flag (a22), UFO deck volume×fee seed (a23), Holder Analytics 0 (a24),
ErrorBoundary reset (a26).

**Downgraded:** G's "PLS falls back to $0.00005" from P0 to P1 (a5) — live page had a real PLS price.
**Not added:** nothing that duplicates u3/g1–g6/b1–b9, nothing on the gotcha list.

**Roadmap artifact:** republished (version 6): AUDIT II phase card, Next-up rewritten for the current
state (it still said "Deploy Phase 0"), snapshot stats refreshed, "Seen live on Sep 16" list, phase grid
3×2, g8 description updated to live. 126 items total, 45 ticked.

No code changed this evening. Next session starts with a1–a4 (all S), then a13/a10/a11/a25.

---

## Night — a1–a4 + a13 knocked out

- **a1 charts.html** `filterToRange`: the +1-day buffer now applies only when `getTier(days)==='daily'`;
  intraday tiers cut at exactly `now − days`. 24H/7D/14D windows for PTGC/UFO/WPLS now match BTC/ETH.
- **a2 ledger.html** direct `transfer()` branch: amount shown = sum of the token-transfer records to the
  recipient for that hash (calldata only as fallback); symbol/decimals from the record when the token
  is not in KNOWN_TOKENS; a "Transfer call: 45.00M PTGC (fee on transfer took the rest)" detail line when
  they differ. September burns 307.00M → **293.19M**; "Sent 4.19M" → 3.98M.
- **a4 ledger.html** buy branch: `ptgcReceivedTotal` sums every PTGC transfer into the DAO wallets for the
  hash. Sep 15 buy 634.48K → **48.29M** (= the DAO Buys chart); September PTGC Buys 315.45M → **363.11M**.
- **a3 scripts/fetch-coingecko-data.js**: `d90` now comes from `changeFromDailySeries(name, 90)` — the
  repo's own `data/charts-data.json` daily series, same arithmetic as index's `_chgAt` — instead of
  CoinGecko's 200-day field; null when the series is younger than 90 d (UFO). Preview from today's file:
  PTGC −33.4% (was −53.1% shown as "90D"), WPLS +65.7%, PLSX +97.6%, HEX +253.9%, EHEX +134.7%, INC +70.5%.
  Takes effect at the next `fetch-coingecko-data` run (or Run workflow by hand).
- **a13** calculators / portfolio / charts / ledger: every CDN script pinned to the exact versions
  index.html uses, with `integrity` + `crossorigin`: react 18.3.1, react-dom 18.3.1, @babel/standalone
  7.26.4, chart.js 4.4.1 `chart.umd.js` (charts + calculators switched off the on-the-fly-minified
  `chart.umd.min.js`), chartjs-adapter-date-fns 3.0.0 (new hash, from the npm tarball), html2canvas
  1.4.1 (charts only). **All six hashes were verified against the bytes the CDNs serve** (SubtleCrypto in
  the browser pane). The unused html2canvas tags on calculators and portfolio are gone (−200 KB each).
  Tailwind pinned to /3.4.16 (no SRI possible). All three React pages compile under Babel 7.26.4
  (`node compile.js ../../<page>.html`).
- Verified: `HTML=ledger.html node run.js "" 1280 900 out/ledger` mounts the ledger in the harness (the
  CDN tags are now identical to index's, so the rewrite matches) — September summary reads 8 buys 363.11M,
  7 burns 293.19M, "Bought 48.29M PTGC", no console errors. Charts 24H can only be judged live
  (GeckoTerminal) — check after deploy: PTGC and BTC series should both start ~24 h back.
- Roadmap: a1, a2, a3, a4, a13 ticked. 50 of 126.

---

## Late — a10, a11, a25

- **a10 portfolio.html**: `isValidAddr` + `sanitizeWallets(list)` → `{wallets,dropped}`; used by the
  `wallets` useState initialiser (a poisoned localStorage list is cleaned on read and re-saved clean)
  and by `decodePortfolio` (a Load Code with no valid address → "That code holds no valid wallet
  addresses"; partial codes load the valid ones and warn in the console). New `PortfolioBoundary`
  class around `PortfolioPage` with Reload and "Clear saved wallets & reload". Harness: a list with
  `{n:'broken'}`, one valid wallet and `{a:'not-an-address'}` → page renders, one chip, storage
  re-saved with 1 entry, no errors.
- **a11 portfolio.html**: in `fetchPortfolioData` the six PTGC `eth_call` words are checked with
  `okWord` (≥ 66 chars, not '0x'); any miss throws. `fetchWalletData` stores `{error,at}` for the
  wallet instead of data — the aggregate and the breakdown skip it, the chip shows a red dot and a
  "read failed · retry" button, the auto-fetch guard still sees an entry so nothing hammers the RPC,
  and Refresh clears it. Harness `RPC_DOWN=1`: chip + retry button, totals untouched, no data cached.
- **a25 index.html** `AbductionDeckModal` header: `flex-wrap`; ✕ is `tap` (32×32) with
  `order-1 ml-auto` below sm so it sits top-right on the first line, the token toggle + window pills
  drop to a full-width second line (`order-2 basis-full`); on sm+ `sm:order-3` / `sm:basis-auto
  sm:ml-auto` restore the single row. Harness 375×812: ✕ at 331–363, all five pills inside the
  viewport, ✕ click closes the deck; 1440: one row, ✕ last at 1254–1286.
  New probe `probes/deck-header.js` (geometry of the deck header + off-screen button count).
- Compiles: index (783,819 B), portfolio (103,573 B). Roadmap: a10, a11, a25 ticked → 53 of 126.

---

## Last round — a22, a24, a16, a48

- **a22** `AbductionDeckModal` token toggle no longer writes `grays_deck_reopen`; `showBeam` starts
  `false` and a mount effect sweeps the retired flag from tabs that still carry it. Harness: open deck →
  toggle UFO → dialog label "UFO Live Feed", hash `#/ufo`, flag null, Escape closes.
- **a24** `HolderAnalyticsModal`: `cur = holdersByPeriod.current ?? holders ?? null` (was `||0`), the
  chart's synthetic "now" point is only appended when that count is known and > 0, and
  `holdersByPeriod.current` is in the chart effect's deps. `fmt(null)` prints "—"; `calcChange` already
  guards null. Note: in the harness `PS_DOWN=1` still shows 18,072 because `holder-history.json`
  supplies `holdersByPeriod.current` — the null path needs both PulseScan and the file to miss.
- **a16** DAO Treasury panel: the four tiles print "—" (and no "0 PLS" / "(0)" sub-lines) when
  `daoData` is null, matching the share card. Root cause fixed too: `fetchDAOData` returned a zeros
  object when `eth_getBalance` came back `{result:null}` (dead RPC pool) — it now returns null.
  Harness `RPC_DOWN=1`: all four dashes; RPC up (stub now answers `eth_getBalance`): numbers.
- **a48** `SubPrice` (module scope, next to `Price`): every sub-micro price renders
  `<span class="sr-only">$0.00004542</span>` + the visual notation `aria-hidden select-none`, so a
  screen reader announces and a copy yields the full decimal; `fullPriceText` scales its decimals to
  the magnitude. `Price`, `formatSubPrice` and `PriceEl` all go through it. `shareCardPng`'s onclone
  removes `.sr-only` inside `[data-share-wrap]` so html2canvas never paints the hidden text —
  verified with `H2C=1 DS_PRICE=4.542e-5` on the Targets card (2400×1350 PNG, notation intact, no
  stray text). Harness: `probes/price-copy.js` → copied "$0.00004542", painted "$0.0₄4542".
- Harness: `eth_getBalance` stub, `PS_DOWN=1`, `DS_PRICE=n`; probes `dao-panel.js`,
  `holder-modal.js`, `price-copy.js`; `share-png-real.js` now prefers the card's own
  `aria-label="Download PNG"` button (on the Socials tab the "Download official logos" buttons came
  first in DOM order and the probe clicked one of those).
- Roadmap: a16, a22, a24, a48 ticked → 57 of 126.
