name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:
  workflow_run:
    workflows:
      - "Fetch Treasury Transactions"
      - "Fetch UFO PTGC Burns"
      - "Fetch CoinGecko Data"
      - "Update Burn and Holder History"
      - "Update Metrics History"
      - "LV Snapshot"
    types: [completed]

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  # Allow only one Pages deployment at a time. Queue new ones behind the
  # running deploy and skip intermediate queued runs (GitHub keeps only the
  # latest). Do NOT cancel an in-progress deploy — cancelling one mid-flight
  # is what was showing up as failed runs when several data workflows finished
  # at once and each triggered a deploy. This matches GitHub's official Pages
  # deployment template.
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    # When triggered by workflow_run, only deploy if the upstream fetch succeeded.
    # For push / workflow_dispatch triggers, this condition passes trivially.
    if: ${{ github.event_name != 'workflow_run' || github.event.workflow_run.conclusion == 'success' }}
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: '.'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
